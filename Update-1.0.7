Version 1.0.6:

After a long coding session, several late night hours....

* Fixed the way the add mods content manager listener manges which file to install for said instance based on moadloader the instance is set to. This new system fix is a WIP so if something fails please make an issue report
+ Added the system to change instance locations and a way for it to move existing instances to the new one from the old instance folder.
+ Added to the instances where it'll cache some inof on modpacks you install from discover/discovery so that it can properly display its icon and be able to fetch versions and upldate them accordingly. Note Downloaded instances update button differes from custom made instances.
+ Added a servers tab But this system is heavily WIP and may not stick around. But you can add a server to an instance via this way But you have to select version and the instance put in the server ip then save/add.

Upcoming changes:

= A rewrite of everything in a newer performance friendly enviorment for 1.0.8. This is gonna take time.
= More features that'll be kept confidental until release of 1.0.8 to 1.0.9
