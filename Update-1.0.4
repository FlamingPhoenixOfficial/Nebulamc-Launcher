Version 1.0.4:

+ Added an update checker and applying system for the launcher, see the launcher settings icon.

# Things to come:
= Now need to add Beta versions for Fabric and quilt where it has ones displaying for older versions.
= add a way for when the user downloads packs from the discover it'll apply its icon/profile picture accordingly to its instance in the instance tab.
= Implement a way for modded instances that are downloaded from the discover to check and fetch to see if it has an update and to make sure it'll apply the update version accordingly if said user clicks update pack.
